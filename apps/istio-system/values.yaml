istiod:
  global:
    istioNamespace: istio-system

gateway:
  name: istio-ingress
  global:
    istioNamespace: istio-system
  service:
    type: NodePort
  autoscaling:
    enabled: false

base: {}
